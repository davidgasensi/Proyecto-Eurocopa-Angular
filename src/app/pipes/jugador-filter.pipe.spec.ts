import { JugadorFilterPipe } from './jugador-filter.pipe';

describe('JugadorFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new JugadorFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
